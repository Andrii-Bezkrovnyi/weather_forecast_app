[pytest]
asyncio_mode = auto
; makes the project root importable during test collection, so
; "from weather_forecast.client import ..." resolves even though
; tests/ has no __init__.py of its own.
pythonpath = .
testpaths = tests
